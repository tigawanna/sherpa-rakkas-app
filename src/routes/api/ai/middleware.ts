import { RequestContext } from "rakkasjs";

export default async function ratelimit(ctx: RequestContext) {
  // const body = await ctx.request.json()
  // console.log("=========== AI MIDDLEWARE ============== ",ctx.request.body)
  // const readable_stream= ctx.request.body
  // if(!readable_stream){
  //   return {
  //     error: {
  //       message: "body is required",
  //       original_error: new Error("body is required"),
  //     },
  //   }
  // }

  return
    // await ctx.next()
  // try {
  //   const user_id = body?.input?.user_id
  //   // console.log("API MIDDLEWRAEhas user id , IS VALID", user_id)
  //   return
  // } catch (error) {
  //   return {
  //     error: {
  //       message: "user id is required",
  //       original_error: new Error("user id is required"),
  //     },
  //   }
  // }
}
